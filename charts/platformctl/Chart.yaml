apiVersion: v2
name: platformctl
description: GitOps-optimized application monitoring platform (gateway, aggregator, and integration services)
type: application
# Chart version — bump on chart changes.
version: 0.1.0
# App version — informational; the deployed image tag is set at deploy time via --set image.tag.
appVersion: "1.0.0"
keywords:
  - gitops
  - argocd
  - vault
  - monitoring
home: https://github.com/kriipke/platformctl
sources:
  - https://github.com/kriipke/platformctl
maintainers:
  - name: platformctl
