apiVersion: v1
kind: Namespace
metadata:
  name: platformctl-services
---
# ConfigMap for common service configuration
apiVersion: v1
kind: ConfigMap
metadata:
  name: platformctl-config
  namespace: platformctl-services
data:
  DATABASE_URL: "postgres://platformctl:platformctl@postgres:5432/platformctl?sslmode=disable"
  RABBITMQ_URL: "amqp://platformctl:platformctl@rabbitmq:5672/"
  LOG_LEVEL: "info"
  ENABLE_METRICS: "true"
---
# Environment Validation Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: environment-validation-svc
  namespace: platformctl-services
spec:
  replicas: 2
  selector:
    matchLabels:
      app: environment-validation-svc
  template:
    metadata:
      labels:
        app: environment-validation-svc
    spec:
      containers:
      - name: environment-validation-svc
        image: ghcr.io/kriipke/platformctl-environment-validation-svc:latest
        ports:
        - containerPort: 8080
        env:
        - name: DATABASE_URL
          valueFrom:
            configMapKeyRef:
              name: platformctl-config
              key: DATABASE_URL
        - name: RABBITMQ_URL
          valueFrom:
            configMapKeyRef:
              name: platformctl-config
              key: RABBITMQ_URL
        - name: LOG_LEVEL
          valueFrom:
            configMapKeyRef:
              name: platformctl-config
              key: LOG_LEVEL
        - name: VAULT_ENABLED
          value: "true"
        - name: VAULT_ADDRESS
          value: "https://vault.example.com"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
        readinessProbe:
          httpGet:
            path: /ready
            port: 8080
          initialDelaySeconds: 5
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: environment-validation-svc
  namespace: platformctl-services
spec:
  selector:
    app: environment-validation-svc
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
---
# App Sync Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-sync-svc
  namespace: platformctl-services
spec:
  replicas: 2
  selector:
    matchLabels:
      app: app-sync-svc
  template:
    metadata:
      labels:
        app: app-sync-svc
    spec:
      containers:
      - name: app-sync-svc
        image: ghcr.io/kriipke/platformctl-app-sync-svc:latest
        ports:
        - containerPort: 8080
        env:
        - name: DATABASE_URL
          valueFrom:
            configMapKeyRef:
              name: platformctl-config
              key: DATABASE_URL
        - name: RABBITMQ_URL
          valueFrom:
            configMapKeyRef:
              name: platformctl-config
              key: RABBITMQ_URL
        - name: LOG_LEVEL
          valueFrom:
            configMapKeyRef:
              name: platformctl-config
              key: LOG_LEVEL
        - name: ARGOCD_ENABLED
          value: "true"
        - name: ARGOCD_SERVER_URL
          value: "https://argocd.example.com"
        - name: ARGOCD_NAMESPACE
          value: "argocd"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
        readinessProbe:
          httpGet:
            path: /ready
            port: 8080
          initialDelaySeconds: 5
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: app-sync-svc
  namespace: platformctl-services
spec:
  selector:
    app: app-sync-svc
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
---
# Context Correlation Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: context-correlation-svc
  namespace: platformctl-services
spec:
  replicas: 2
  selector:
    matchLabels:
      app: context-correlation-svc
  template:
    metadata:
      labels:
        app: context-correlation-svc
    spec:
      containers:
      - name: context-correlation-svc
        image: ghcr.io/kriipke/platformctl-context-correlation-svc:latest
        ports:
        - containerPort: 8080
        env:
        - name: DATABASE_URL
          valueFrom:
            configMapKeyRef:
              name: platformctl-config
              key: DATABASE_URL
        - name: RABBITMQ_URL
          valueFrom:
            configMapKeyRef:
              name: platformctl-config
              key: RABBITMQ_URL
        - name: LOG_LEVEL
          valueFrom:
            configMapKeyRef:
              name: platformctl-config
              key: LOG_LEVEL
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
        readinessProbe:
          httpGet:
            path: /ready
            port: 8080
          initialDelaySeconds: 5
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: context-correlation-svc
  namespace: platformctl-services
spec:
  selector:
    app: context-correlation-svc
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
---
# Multi-Environment Kubernetes Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: multi-environment-kube-svc
  namespace: platformctl-services
spec:
  replicas: 2
  selector:
    matchLabels:
      app: multi-environment-kube-svc
  template:
    metadata:
      labels:
        app: multi-environment-kube-svc
    spec:
      containers:
      - name: multi-environment-kube-svc
        image: ghcr.io/kriipke/platformctl-multi-environment-kube-svc:latest
        ports:
        - containerPort: 8080
        env:
        - name: DATABASE_URL
          valueFrom:
            configMapKeyRef:
              name: platformctl-config
              key: DATABASE_URL
        - name: RABBITMQ_URL
          valueFrom:
            configMapKeyRef:
              name: platformctl-config
              key: RABBITMQ_URL
        - name: LOG_LEVEL
          valueFrom:
            configMapKeyRef:
              name: platformctl-config
              key: LOG_LEVEL
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
        readinessProbe:
          httpGet:
            path: /ready
            port: 8080
          initialDelaySeconds: 5
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: multi-environment-kube-svc
  namespace: platformctl-services
spec:
  selector:
    app: multi-environment-kube-svc
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
---
# Customer Git Branch Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: customer-git-branch-svc
  namespace: platformctl-services
spec:
  replicas: 2
  selector:
    matchLabels:
      app: customer-git-branch-svc
  template:
    metadata:
      labels:
        app: customer-git-branch-svc
    spec:
      containers:
      - name: customer-git-branch-svc
        image: ghcr.io/kriipke/platformctl-customer-git-branch-svc:latest
        ports:
        - containerPort: 8080
        env:
        - name: DATABASE_URL
          valueFrom:
            configMapKeyRef:
              name: platformctl-config
              key: DATABASE_URL
        - name: RABBITMQ_URL
          valueFrom:
            configMapKeyRef:
              name: platformctl-config
              key: RABBITMQ_URL
        - name: LOG_LEVEL
          valueFrom:
            configMapKeyRef:
              name: platformctl-config
              key: LOG_LEVEL
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
        readinessProbe:
          httpGet:
            path: /ready
            port: 8080
          initialDelaySeconds: 5
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: customer-git-branch-svc
  namespace: platformctl-services
spec:
  selector:
    app: customer-git-branch-svc
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080