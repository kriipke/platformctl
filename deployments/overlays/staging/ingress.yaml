apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: contextops-ingress
  namespace: contextops-staging
  labels:
    app.kubernetes.io/name: contextops-ingress
    app.kubernetes.io/part-of: contextops-platform
    app.kubernetes.io/environment: staging
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/cors-allow-origin: "https://staging.contextops.com,https://contextops-staging.example.com"
    nginx.ingress.kubernetes.io/cors-allow-methods: "GET, POST, PUT, DELETE, OPTIONS"
    nginx.ingress.kubernetes.io/cors-allow-headers: "DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range,Authorization"
    nginx.ingress.kubernetes.io/enable-cors: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/limit-rps: "50"
    cert-manager.io/cluster-issuer: "letsencrypt-staging"
    # Staging-specific annotations
    nginx.ingress.kubernetes.io/configuration-snippet: |
      more_set_headers "X-Environment: staging";
      more_set_headers "X-Frame-Options: SAMEORIGIN";
      more_set_headers "X-Content-Type-Options: nosniff";
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - staging.contextops.com
    - contextops-staging.example.com
    secretName: contextops-staging-tls
  rules:
  - host: staging.contextops.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: staging-contextops-gateway
            port:
              number: 80
  - host: contextops-staging.example.com
    http:
      paths:
      # API endpoints
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: staging-contextops-gateway
            port:
              number: 80
      # Health and debug endpoints
      - path: /health
        pathType: Prefix
        backend:
          service:
            name: staging-contextops-gateway
            port:
              number: 80
      - path: /debug
        pathType: Prefix
        backend:
          service:
            name: staging-contextops-gateway
            port:
              number: 80
      # Test routes (staging only)
      - path: /test
        pathType: Prefix
        backend:
          service:
            name: staging-contextops-gateway
            port:
              number: 80
---
# Separate metrics ingress for staging monitoring
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: contextops-metrics-ingress
  namespace: contextops-staging
  labels:
    app.kubernetes.io/name: contextops-metrics-ingress
    app.kubernetes.io/part-of: contextops-platform
    app.kubernetes.io/environment: staging
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/auth-type: basic
    nginx.ingress.kubernetes.io/auth-secret: contextops-metrics-auth
    nginx.ingress.kubernetes.io/auth-realm: "ContextOps Staging Metrics"
    cert-manager.io/cluster-issuer: "letsencrypt-staging"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - metrics-staging.contextops.com
    secretName: contextops-staging-metrics-tls
  rules:
  - host: metrics-staging.contextops.com
    http:
      paths:
      - path: /gateway
        pathType: Prefix
        backend:
          service:
            name: staging-contextops-gateway
            port:
              number: 9090
      - path: /aggregator
        pathType: Prefix
        backend:
          service:
            name: staging-contextops-gitops-aggregator
            port:
              number: 9090